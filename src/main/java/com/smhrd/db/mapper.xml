<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.smhrd.db.mapper">
<!-- id : dao 호출 시 매칭을 위해 사용하는 id 값 (모든 태그에 필수) -->
<!-- parameterType : dao에서 넘겨주는 데이터, sql 완성 시 필요한 데이터 -->
<!-- #{?} {}중 괄호는 객체 안 필드에 만들어준 이름 작성 -->

<!-- 회원가입(insert)  -->
<!-- <insert id="join" parameterType="userVO">
	insert into tb_user values(#{id},#{pw},#{name},#{age},#{salary},#{nick},sysdate)
</insert>

  로그인(select) 
 <select id="login" resultType ="userVO" parameterType="userVO">
 	select * from tb_user where user_id = #{id} and pw = #{pw}
 </select>
 -->
</mapper>